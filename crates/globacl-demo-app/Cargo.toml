[package]
name = "globacl-demo-app"
version = "0.1.0"
edition.workspace = true
license.workspace = true
repository.workspace = true

[dependencies]
globacl-agent = { path = "../globacl-agent" }
globacl-core = { path = "../globacl-core" }
