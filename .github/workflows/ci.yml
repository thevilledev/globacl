name: ci

on:
  push:
    branches:
      - main
  pull_request:

permissions:
  contents: read

concurrency:
  group: ci-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

env:
  CARGO_TERM_COLOR: always
  RUST_BACKTRACE: 1

jobs:
  rust:
    name: rust
    runs-on: ubuntu-24.04
    timeout-minutes: 20
    steps:
      - name: Checkout
        # actions/checkout v6.0.2
        uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd
        with:
          persist-credentials: false

      - name: Install pinned Rust toolchain
        run: rustup toolchain install

      - name: Cargo cache
        # actions/cache v5.0.5
        uses: actions/cache@27d5ce7f107fe9357f9df03efb73ab90386fccae
        with:
          path: |
            ~/.cargo/git
            ~/.cargo/registry
            target
          key: cargo-${{ runner.os }}-${{ hashFiles('rust-toolchain.toml', 'Cargo.lock', 'Cargo.toml', 'crates/**/Cargo.toml') }}
          restore-keys: |
            cargo-${{ runner.os }}-

      - name: Check formatting
        run: cargo fmt --all --check

      - name: Clippy
        run: cargo clippy --workspace --all-targets --locked -- -D warnings

      - name: Test
        run: cargo test --workspace --locked

      - name: Benchmark harness
        run: cargo run -p globacl-bench --release --locked -- --ci

      - name: Build
        run: cargo build --workspace --locked

  repository:
    name: repository
    runs-on: ubuntu-24.04
    timeout-minutes: 10
    permissions:
      actions: read
      contents: read
    steps:
      - name: Checkout
        # actions/checkout v6.0.2
        uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd
        with:
          persist-credentials: false

      - name: Check shell scripts
        run: |
          set -euo pipefail
          bash -n deploy/k3s/local-smoke.sh
          bash -n deploy/k3s/jetstream-smoke.sh
          bash -n deploy/k3s/global-smoke.sh
          bash -n deploy/k3s/observability-smoke.sh

      - name: Install uv
        # astral-sh/setup-uv v8.1.0
        uses: astral-sh/setup-uv@08807647e7069bb48b6ef5acd8ec9567f424441b
        with:
          version: "0.11.7"
          cache-dependency-glob: uv.lock

      - name: Lint YAML
        uses: ./.github/actions/yamllint

      - name: Validate OpenAPI
        run: |
          set -euo pipefail
          uv run --frozen openapi-spec-validator docs/openapi.yaml

      - name: Install pnpm
        run: npm install --global pnpm@11.2.2

      - name: Check generated clients
        env:
          OAPI_CODEGEN_VERSION: "v2.5.0"
        run: scripts/check-generated-clients.sh

      - name: Test Go clients
        working-directory: clients/go
        run: go test ./...

      - name: Audit GitHub Actions
        # zizmorcore/zizmor-action v0.5.6
        uses: zizmorcore/zizmor-action@5f14fd08f7cf1cb1609c1e344975f152c7ee938d
        with:
          advanced-security: false
          annotations: true

      - name: Check diffs
        run: git diff --check

  docker:
    name: docker
    runs-on: ubuntu-24.04
    timeout-minutes: 20
    steps:
      - name: Checkout
        # actions/checkout v6.0.2
        uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd
        with:
          persist-credentials: false

      - name: Build image
        run: docker build -t ghcr.io/thevilledev/globacl:ci .
