[project]
name = "globacl-ci"
version = "0.0.0"
requires-python = ">=3.11"
dependencies = [
    "openapi-spec-validator==0.8.5",
    "yamllint==1.38.0",
    "zizmor==1.25.2",
]

[tool.uv]
package = false
